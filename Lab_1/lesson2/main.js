const app = Vue.createApp({       // Vue.createApp({option object}) 
    data() {
        return {
            title : "Nghi's Shop", // challenge
            product : 'Socks',  // updated data value // task
            description : 'These soft socks keep my feet warm on chilly mornings.', // challenge
        }
    }
})
